version: 2.1
orbs:
  node: circleci/node@3.0.0
workflows:
  node-tests:
    jobs:
      - run:
            name: MongoDB Install
            command: |
              sudo apt-get install -y mongodb
      - node/test
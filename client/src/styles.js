import styled from 'styled-components';

const PhotosWrapper = styled.div`
  display: flex;
`;

const PrimaryPhoto = styled.img`
  max-width: 100%;
  height: auto;
`;

export {
  PhotosWrapper,
  PrimaryPhoto
}